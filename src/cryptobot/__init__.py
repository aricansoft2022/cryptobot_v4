"""Deterministic RSI / RSI-VWMA / ADX / ADR trading strategy core.

This package implements the exact, deterministic trading strategy described in
the project specification. The rules are intentionally fixed: no extra
indicators, no additional filters, no stop-loss / trailing-stop, no alternative
crossover or entry/exit logic. See ``README.md`` for the full spec-to-code map.
"""

__version__ = "0.1.0"
